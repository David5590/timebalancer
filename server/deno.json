{
  "tasks": {
    "start": "deno run -A --watch=server.ts server.ts",
    "test": "deno lint && deno check main.ts && deno fmt --check && deno test -A --unstable --no-check tests/",
    "deploy": "deployctl deploy --project=timebalancer ./server.ts --prod"
  }
}
